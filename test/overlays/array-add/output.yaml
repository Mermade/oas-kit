openapi: 3.1.0
info:
  title: API
  version: 1.0.0
paths:
  /:
    get:
      parameters:
      - name: id
        in: query
        schema:
          type: integer
      - name: newParam
        in: query
        schema:
          type: string
      responses:
        '200':
          description: OK
