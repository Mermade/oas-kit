swagger: "2.0"
info:
  title: API
  version: 1.0.0
paths:
  /:
    post:
      consumes:
        - application/json
      parameters:
        - name: p1
          in: formData
          type: string
      responses:
        default:
          description: OK
