{
    "title": "Id only entity",
    "description": "A sample public model.",
    "required": ["public-id"],
    "type": "object",
    "properties": {
      "paging": {
        "$ref": "paging.json"
      },
      "public-id": {
        "type": "string",
        "example": "some type"
      }
    }
  }